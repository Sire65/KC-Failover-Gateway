{
  "name": "kc-failover-gateway",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "main": "src/worker.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "check": "node --check src/worker.js && node --check netlify/functions/gateway.mjs && node --check sdk/kc-failover-client.js && node tests/client-layer.mjs"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "wrangler": "^4.68.0"
  }
}

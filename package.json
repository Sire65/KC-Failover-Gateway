{
  "name": "kc-failover-gateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/secure-worker.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "wrangler": "^4.68.0"
  }
}
